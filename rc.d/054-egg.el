;;
(eval-when-compile (require 'cl))



;; auto-update egg-status on file save
(setq egg-auto-update t)

;; switch to the status buffer in the same window
(setq egg-switch-to-buffer t)


(provide '054-egg)
;; 054-egg.el ends here.
